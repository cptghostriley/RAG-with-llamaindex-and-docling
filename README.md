# RAG-with-llamaindex-and-docling
Local RAG pipeline for document Q&amp;A. Uses Docling for parsing, Ollama for embeddings/LLM, and cross-encoder reranking. Supports PDF, DOCX, TXT, PPTX. Gradio UI + FastAPI. Fully offline and private - your data never leaves your machine.
